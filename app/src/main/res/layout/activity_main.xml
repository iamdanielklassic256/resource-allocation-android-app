<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".MainActivity">


    <ImageView
        android:id="@+id/appLogo"
        android:layout_width="544dp"
        android:layout_height="428dp"
        android:src="@drawable/logo" />

    <TextView
        android:id="@+id/appName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/appLogo"
        android:layout_marginLeft="80dp"
        android:layout_marginTop="-142dp"
        android:text="Resource Allocation"
        android:textColor="@color/white"
        android:textSize="30sp"
        android:textStyle="bold" />

    <!-- User Profile Pic (ImageView) -->
    <TextView
        android:id="@+id/slogan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/appName"
        android:layout_marginLeft="90dp"
        android:text="Discover Efficiency, Unleash Potential."
        android:textColor="@color/white"
        android:textSize="15sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/appLogo"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:orientation="horizontal">

        <!-- User Profile Pic (ImageView) -->
        <ImageView
            android:id="@+id/userProfilePic"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/user11"
            android:layout_gravity="center_vertical" />

        <!-- Inner Linear Layout for User Name and Student Number -->
        <LinearLayout
            android:layout_width="278dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:orientation="vertical">

            <!-- User Name (TextView) -->
            <TextView
                android:id="@+id/usernameLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="5dp"
                android:text="JACWICONGEYO MERCY"
                android:textColor="@color/white"
                android:textSize="20sp"
            android:drawablePadding="10dp"
                tools:ignore="HardcodedText" />

            <!-- Student Number (TextView) -->
            <TextView
                android:id="@+id/studentNumberLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="5dp"
                android:text="200100060"
                android:textColor="@color/white"
                android:textSize="20sp"
                android:drawablePadding="20dp" />

        </LinearLayout>

    </LinearLayout>
    <!-- User Name (EditText) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/getStartedBtn"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="70dp"
        android:backgroundTint="@color/blue"
        android:text="Get Started" />
</RelativeLayout>
